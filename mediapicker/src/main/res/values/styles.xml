<resources>

    <style name="MediaPickerTheme" parent="Theme.AppCompat">
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
        <item name="actionMenuTextColor">@color/picker_color</item>
        <item name="android:textSize">14sp</item>
        <item name="android:divider">@android:color/white</item>
        <item name="actionBarStyle">@style/Toolbar</item>
    </style>

    <style name="Toolbar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/flatBlackDark</item>
        <!-- Support library compatibility -->
        <item name="background">@color/flatBlackDark</item>
    </style>

</resources>